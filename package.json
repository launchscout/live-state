{
  "name": "live-state",
  "version": "0.0.1",
  "description": "An experiment in web app state management",
  "main": "build/live-state.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "tsc",
    "prepublish": "npm run build"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "typescript": "^4.2.2"
  },
  "dependencies": {
    "phoenix": "^1.5.8"
  }
}
